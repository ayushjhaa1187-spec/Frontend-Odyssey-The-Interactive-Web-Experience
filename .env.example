# Frontend Odyssey - Environment Variables
# Copy this file to .env and fill in values

# No environment variables are required for the base application.
# The app runs entirely client-side with no backend dependencies.

# Optional: Vercel deployment (auto-configured by Vercel CLI)
# VITE_VERCEL_URL=
